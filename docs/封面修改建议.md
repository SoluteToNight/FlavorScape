为了让 Codex（代码生成 AI）能精准帮你复刻参考图的效果，你需要把模糊的“视觉感受”转化成**结构、布局、像素级样式和缺失组件**的精准指令。

以下为你梳理出具体的对比分析，并直接为你写好了可以直接复制给 Codex 的**结构化 Prompt 指令**。

## 🛠️ 核心差异深度对比与修改点

### 1. 顶部导航栏（Header）—— 结构完全错位

- **参考图**：标准的扁平通栏布局。左侧是 Logo + 品牌名；中间是纯文字导航（首页、风味地图等）；右侧是搜索图标 + “登录/注册”按钮。
- **复刻图**：误做成了“胶囊外壳”的后台管理风格，且缺少了右侧的登录按钮，中间文字变成了药丸背景。
- **调整核心**：去掉胶囊外壳，改为标准的 `flex justify-between` 分布。

### 3. 主体标题与核心标签区（Hero Content）

- **参考图**：英文副标题“FLAVOR ATLAS OF CHINA”右侧有一个**红色印章小图标**；下方 4 个核心功能图标是精致的**单色线条/SVG 图标**。
- **复刻图**：缺少红色印章；4 个图标样式过于扁平、现代化（像后台手机系统的图标），与整体中式复古风不搭。
- **调整核心**：补充印章元素，更换图标为中式白描或极简线条 SVG。

### 4. 地图层、路径连线— 空间感与细节缺失

- **参考图**：

  1. 地图上的路线有明显的**颜色区分**（如北方/西北是红色虚线，江南是绿色虚线），且地名标签（如“北京”、“新疆”）有对应路线颜色的**纯色底框背景**。

  1. 路线颜色单一，标签全变成了白底。

     （补充：那个右下角的茉莉的那个卡片实际上是不能交互的，我觉得可以删去）

- **调整核心**：为不同的 SVG 路径（Path）赋予不同的 `stroke` 颜色和 `stroke-dasharray` ；重构地名标签的 CSS 背景色。

## 📋 方案：你可以直接发给 Codex 的精准提示词 (Prompt)

请将以下这段务实的开发指令直接喂给你的 Codex：

Plaintext

```
你是一个精通前端开发的专家。请根据以下具体的布局和样式修改要求，调整当前页面的 HTML 结构和 CSS 代码（不包含底部数据大卡片部分）：

1. 顶部导航栏 (Header) 重构：
   - 移除当前的水平胶囊/药丸状外壳，改为通栏透明背景（或微弱高光）。
   - 使用 flex 布局进行三段式分布 (justify-between, items-center)。
   - 左侧：保持 Logo 加上“寻味地理 | FLAVOR ATLAS”的组合。
   - 中间：改为纯文字链接列表（首页、风味地图、食材溯源、工艺文明、品牌故事、产业链溯源、智库），字号缩小，取消背景色。
   - 右侧：放置一个搜索图标，并在其右侧增加一个深色圆角按钮，文字为“登录 / 注册”。

3. 主体文案与图标区细节微调：
   - 在英文主标题 "FLAVOR ATLAS OF CHINA" 的末尾，紧跟一个小的红色正方形“印章”图标或 CSS 块。
   - 调整中间 4 个特性图标（食材溯源、工艺文明...）的样式：将当前的圆形描边图标更换为更纤细、复古的单色 SVG 线条图标，增大图标与下方文字的间距。

4. 地图连线与地名标签 (Map & SVG Routes) 精细化：
   - 地图上的连线需要按区域区分颜色：西北-西南线（新疆-河西走廊-四川-云南-福建-广东）使用铁锈红/橙红色虚线；北京-江南-福建线使用墨绿色虚线。
   - 调整地名标签（Label）的样式：标签不再使用统一的浅色边框。
     * “北京”标签背景色改为深红色，文字反白。
     * “新疆”、“河西走廊”、“四川”、“云南”、“广东”、“福建”标签背景色改为铁锈红，文字反白。
     * “江南”标签背景色改为墨绿色，文字反白。

请给出修改后的完整 HTML 和 CSS/Tailwind 代码。
```