你的设计直觉非常敏锐！你红圈圈出来的这副《八大菜系》参考图中的两个核心设计亮点，正是高级信息可视化（Infographics）与普通网页排版的分水岭：

1. **悬浮引线注释（Leader Line Annotations）：** 打破“图是图、字是字”的割裂感。用极细的线条从地图上引出，把文字排布在地图周围的留白处，既不遮挡底图，又精准传达了地理空间关系。
2. **紧凑式蛇形时间轴（S-Curve / Compact Timeline）：** 打破了传统垂直列表极度浪费纵向空间的问题。通过连续弯折的折线（蜿蜒流转）将庞大的信息高密度地折叠在一个小区域内，阅读视线非常连贯。

现在的任务是把这两个“绝招”无缝嫁接到我们的 4 个输出风格里，彻底干掉那些笨重的方块。我已经为你准备好了分模块、带前端工程约束的“降维打击级”沟通文档。

请**每次只复制一个模块**给 Codex，步步为营地重构。

### 📍 模块一：【地理志大屏】—— 引入悬浮引线与全息面板

> **复制给 Codex 的指令：**
>
> “Codex，现在的‘地理志’视图中，粗笨的半透明卡片把中央地图完全压死了，毫无交互体验可言。我们需要引入**『悬浮引线注释』**和**『全息微排版』**来重构大屏交互。请严格遵守以下指令：
>
> 1. **引线标注化（SVG Leader Lines）：** >    禁止使用普通的 HTML 块状卡片直接压在地图节点上。请在地图上方叠加一层透明的 SVG 层，当用户聚焦某个节点时，从该地理坐标点向上/外拉出一条极细的折线（引线），将描述文字（如‘加工节点...’）悬浮在引线末端的空白处，绝对不能遮挡该节点和连接线路。
>
> 2. **极致瘦身右侧面板：**
>
>    右侧的‘溯源叙事线’卡片太宽了。请将其宽度压缩至 `280px` 以内。去掉所有卡片内的色块背景，改用一条极细的左侧边框线（Border-left）作为时间轴主轴，节点之间极其紧凑，利用加粗的数字和极小的文字（12px）形成紧凑的微排版。
>
> 3. **全息透视背景：**
>
>    左右两侧面板彻底抛弃深灰色背景，必须使用极其通透的毛玻璃效果：`background: linear-gradient(90deg, rgba(15,20,25,0.8) 0%, rgba(15,20,25,0) 100%); backdrop-filter: blur(10px);`，让底下的地图地形依然清晰可见。”

### 🏷️ 模块二：【品牌公示海报】—— 拯救圆形地图与紧凑水平流

> **复制给 Codex 的指令：**
>
> “Codex，‘品牌公示’模式犯了一个致命的排版错误：巨大的文本框被硬塞进狭小的圆形地图内部，导致地图完全失去可视化价值。
>
> 请立刻重组视觉重心：
>
> 1. **纯净徽章地图：** 中央的圆形地图内**严禁放入任何说明文字和白色卡片**。它就是一个纯粹展示‘点与线’的地理印章。
> 2. **紧凑式水平时间轴（Compact Horizontal Timeline）：** >    把原本塞在地图里的‘Traceability Route’文字提取出来，放置在圆形地图的正下方。将其转化为一条极简的水平横向时间线。去掉背景色，用一条细长的虚线连接 4 个极小的彩色圆点，下方紧凑排列 12px 的节点名称（产地 → 加工 → 仓储 → 市场）。
> 3. **底部数据阵列：** 下方的‘产地环境、加工工艺’等内容，彻底抛弃细线网格边框。使用无边界的 CSS Grid 布局，标题用重字重（font-weight: 700），内容用极轻字重，靠极简的排版留白来区分区块。”

### ⚙️ 模块三：【产业链图谱】—— 引入 S 型蛇形时间轴

> **复制给 Codex 的指令：**
>
> “Codex，现在的‘产业链’视图只是一条无聊的垂直列表，极其浪费屏幕空间，且毫无设计感。我们需要借鉴顶级信息图表的**『紧凑式蛇形流程线（S-Curve Flowchart）』**。
>
> 请废除现有的垂直单列布局，彻底重写该视图：
>
> 1. **蛇形折叠布局：** 请用纯 CSS 或内联 SVG 构建一条连续的、从上到下蜿蜒弯折的 S 型线条（类似贪吃蛇的轨迹）。
> 2. **交错式节点安置：** 将 5 个溯源节点顺着这条弯曲的轨迹交错排列。比如节点 1 在左上方，线条向右折，节点 2 在右方，线条再向左下折。
> 3. **卡片去物质化：** 去掉节点外面那个硕大无比的米色圆角背景框！所有的标题、数据指标（如水份 < 14.5%）、说明文字，直接以高密度的文字排版形式，‘贴’在 S 型线条的对应节点旁边。整体风格要像一张严密的工业流水线蓝图。”

### 🏛️ 模块四：【风物档案考据】—— 信息高密度折叠

> **复制给 Codex 的指令：**
>
> “Codex，‘风物档案’模式目前左侧像个沉闷的表格，右侧的地图太空旷。我们需要将其重构为具有国家地理级别质感的**学术考据志**。
>
> 1. **左侧：高密度标签云（Capsule Tags）：**
>
>    去掉左侧所有的长句子和段落。将所有风土数据（如土壤、积温、碎米率）重构为高度紧凑的圆角微型标签（类似 `display: inline-flex; border-radius: 4px; background: rgba(0,0,0,0.05);`），让信息呈现出极高的密度和专业感。
>
> 2. **右侧：地图引线考据（Map Annotation）：**
>
>    右侧地图不再只展示简单的连线。请在地图的重点区域（如东北产区、长三角市场），使用类似于博物馆展板的‘SVG 悬浮引线（Leader Lines）’，从地图对应的经纬度拉出一条极细的折线，在屏幕留白处连接一个纯文字的注释块（如‘核心稻作区：拉林河流域’）。打破地图的边界感。”

通过这套组合拳，Codex 会被强迫使用 SVG 连线、绝对定位、毛玻璃和非对称布局等高级技巧，不再敢用烂大街的 `<div>` 方块敷衍你。

在这四个极具挑战的视图重构中，你最想先拿哪一个“开刀”，来测试 Codex 对这套高级布局指令的吸收程度？