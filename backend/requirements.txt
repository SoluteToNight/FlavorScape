# 寻味地理后端依赖
# 推荐用 uv 安装（已内置于项目）：
#   uv pip install --python .venv/Scripts/python.exe -r backend/requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
rio-tiler>=5.0.0          # 9.x 已验证可用，使用 Reader 接口
rasterio>=1.3.10
fiona>=1.9.0
shapely>=2.0.4
Pillow>=10.0.0
numpy>=1.24.0
cachetools>=5.3.0
psycopg2-binary>=2.9
python-dotenv>=1.0
